import React from 'react';
import './App.css';
import CustomPage from './CustomPage';

interface AppProps {}

function App({}: AppProps) {
  return (
    <div>
      <CustomPage />
    </div>
  );
}

export default App;
